mutation addProductsToWishlist($id: ID!, $wishlistItems: [WishlistItemInput!]!) {
    addProductsToWishlist(wishlistId: $id, wishlistItems: $wishlistItems){
        wishlist {
            items_count
        }
    }
}
